export function requirements(state: string, industry: string) {

  return [
    `Register your ${industry} business with the state of ${state}`,
    `Obtain a general business license`,
    `Apply for local city permits depending on your location`,
    `Register for state taxes if required`,
    `Pass inspections depending on the type of ${industry}`
  ]

}